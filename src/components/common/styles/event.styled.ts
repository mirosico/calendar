import styled from 'styled-components';

export const StyledEvent = styled.div`
    width: 100%;
    .event {
        margin: 2px 0;
        width: 100%;
        padding: 0;
        height: auto;
        min-height: 20px;
    }
`;
